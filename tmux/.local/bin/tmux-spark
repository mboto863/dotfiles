#!/usr/bin/env bash
# bin/tmux-spark

SESSION="spark-workspace"

# Start a new detached tmux session
tmux new-session -d -s $SESSION

# Name first window and start pyspark
tmux send-keys -t $SESSION:0.0 "uv run pyspark" C-m

# Resource monitor to watch RAM/CPU
